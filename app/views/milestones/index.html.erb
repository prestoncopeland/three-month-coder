<div class="container">

<h1 class="boxed">My Coding Progress</h1>

<p>Total hours so far: <strong><%= @total_hours %></strong> </p>

<p>Below is a table of exactly what I’ve been learning and for how long I’ve been learning it.</p>

<table class="gridtable">
  <thead>
    <tr>
      <th>DAY</th>
      <th>DATE</th>
      <th>TIME(S)</th>
      <th>HOURS</th>
      <th>ACTIVITIES</th>
      <% if logged_in? %>
      <th></th>
      <th></th>
      <% end %>
    </tr>
  </thead>

  <tbody>
    <% @milestones.each do |milestone| %>

      <tr>
        <td><%= milestone.date_worked.strftime('%a') %> </td>
        <!-- the +0 below is to remove padding zero for the day because Mac doesn't support the %-d method-->
        <td><%= milestone.date_worked.strftime('%-m-%-d-%Y') %> </td>
        <td><%= milestone.time %> </td>
        <td><%= milestone.hours %> </td>
        <td><%= milestone.activity %> </td>
        <% if logged_in? %>
        <td><%= link_to 'Edit', edit_milestone_path(milestone) %></td>
        <td><%= link_to 'Destroy', milestone, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <% end %>
      </tr>

    <% end %>
  </tbody>
</table>

<% if logged_in? %>
<p> <%= link_to "Record A New Milestone", new_milestone_path %>
</p>
<% end %>

</div>